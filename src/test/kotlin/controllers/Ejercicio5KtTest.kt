package controllers

import org.example.controllers.menu
import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*

//No se como realizar los test de este problema, ya que al haber utilizado una lista no considero que se pueda llegar a realizar un test de importancia, pero puedo estar claramente equivocado.